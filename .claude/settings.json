{
  "hooks": {
    "Stop": [
      {
        "statusMessage": "Auto-salvando alterações no GitHub...",
        "hooks": [
          {
            "type": "command",
            "command": "git add -A && git diff --staged --quiet || (git commit -m 'chore: auto-commit via Claude Code hook' && git push)",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
